# # json.set! @queue.film_id do
# #     json.extract! @list.film, :title, :year, :rating, :description
# #     json.url url_for(@list.film.film)
# #     json.poster url_for(@list.film.poster)
# #     json.extract! @list, :id, :user_id, :film_id
# # end

# json.extract! @queue, :film_id